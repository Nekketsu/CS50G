@inject IJSRuntime JSRuntime

<canvas class="@(isFullScreen ? "fullscreen" : null)" width="@(Width * SuperSampling)" height="@(Height * SuperSampling)" @ref="canvas" @attributes="FullScreenAttributes">
</canvas>
