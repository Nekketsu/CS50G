namespace BreakoutGame
{
    public enum RowPattern
    {
        Solid,     // all colors the same in this row
        Alternate, // alternate colors
        Skip,      // skip every other block
        None       // no blocks this row
    }
}
